spring.application.name=backend

# Database Configuration
# Note: These will be overridden by environment variables in docker-compose.yml
spring.datasource.url=jdbc:postgresql://localhost:5432/patienttriage
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Open AI key - reads from environment variable (set in .env file)
#openai.api.key=${OPENAI_API_KEY}

